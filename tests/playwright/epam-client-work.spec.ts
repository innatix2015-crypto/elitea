import { test, expect } from '@playwright/test';

// EPAM Client Work navigation test - skeleton
test.describe('EPAM client work navigation', () => {
  test('navigates from home to Client Work and verifies text', async ({ page }) => {
    // STEPS_GO_HERE
  });
});
